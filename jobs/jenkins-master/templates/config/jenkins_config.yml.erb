jenkins:
  agentProtocols:
  - "JNLP4-connect"
  - "Ping"
  crumbIssuer:
    standard:
      excludeClientIPFromCrumb: false
  labelString: <%= p('jenkins.master.labels') %>
  mode: <%= p('jenkins.master.mode').upcase %>
  numExecutors: <%= p('jenkins.master.executors') %>
  remotingSecurity:
    enabled: true
  slaveAgentPort: 50000
plugins:
  sites:
  - id: "default"
    url: "https://updates.jenkins.io/update-center.json"
security:
  remotingCLI:
    enabled: false
unclassified:
  location:
    url: "http://<%= spec.ip %>:<%= p('jenkins.master.http_port') %>"
tool:
  git:
    installations:
    - name: "git"
      home: "/var/vcap/packages/git-client/bin"
  jdk:
    installations:
    - name: "jdk"
      home: "/var/vcap/packages/oracle-jdk"
